class Solution {
    public int largestAltitude(int[] gain) {
    int maxsum=0,cursum=0;
    for(int i=0 ; i<gain.length ; i++){
     cursum+=gain[i];
     if(cursum>maxsum)
        maxsum=cursum;
     }
    return maxsum;
    }
}
